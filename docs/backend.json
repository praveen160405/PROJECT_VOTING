{
  "entities": {
    "Vote": {
      "title": "Vote",
      "description": "Represents a single vote cast by a user for a candidate.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who cast the vote."
        },
        "candidateId": {
          "type": "string",
          "description": "The ID of the candidate who received the vote."
        },
        "votedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the vote was cast."
        }
      },
      "required": ["userId", "candidateId", "votedAt"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/votes/{voteId}": {
      "schema": {
        "$ref": "#/entities/Vote"
      },
      "description": "Stores individual vote records, keyed by a unique vote ID."
    }
  }
}
